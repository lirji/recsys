# CLAUDE.md

本文件为 Claude Code (claude.ai/code) 在本仓库中处理代码时提供指导。

## 这是什么

一个面向生产的推荐系统脚手架：Java + Spring Boot 3.2，Maven 多模块单仓库。它实现了经典的 **召回 → 排序 → 重排** 漏斗，并通过离线/近线链路（embedding、协同过滤、模型训练）向在线存储灌入数据。设计为可通过 `docker compose` 完全本地运行，但其结构便于后续拆分为微服务。

**当前状态：核心漏斗已实现并在本地验证（M1–M3）。** 召回（12 个通道：vector/i2i/hot/tag + u2u/swing/semantic/cold + two_tower + generative + lexical + tiger——two_tower 是 DSSM 风格的学习型召回：item 塔离线烘焙进 pgvector，user 塔在线通过 ONNX 运行；generative 是 RQ-VAE 语义 ID 召回(TIGER 范式可服务版)：离线把 item 量化成 codeword 元组灌 `item_semantic_id`，在线按用户近期 item 的语义 ID 前缀检索同簇，见 docs/04 §14；lexical 是词法/BM25 召回(Postgres 全文检索 `ts_rank_cd` over `item.title_tsv`)，搜索场景与 semantic 向量路经 RRF 混合检索，见 docs/04 §11;tiger 是**完整 TIGER 自回归生成召回**(`train_tiger.py` 训 decoder-only Transformer→`tiger.onnx`,在线 `TigerRecaller` 读历史语义 ID→beam search 生成下一个 item 的语义 ID→映射回 item,见 docs/04 §14.1),与 generative(前缀检索版)并存）、排序（rule + LightGBM→ONNX + DeepFM + MMoE 多任务/ESMM + DIN 序列 + DIEN 兴趣演化 + SIM 长序列 + 双塔粗排）、可插拔重排（diversity/mmr/dpp/none）、分层 A/B 实验（recall×rank×rerank，确定性分桶，曝光记录到 `user_behavior.bucket`；另有 `ad` 层用于**广告分层 A/B**——`ExperimentService.adVariant` 单独分桶、不混入推荐 bucketTag，变体覆盖 reserve-price 等,`ad_event.ad_bucket` 落库供 `ad-report` 按桶对比）、冷启动（检测器 + 兴趣引导）均已就位。离线任务向在线存储灌入数据。`docs/` 中的设计文档是事实来源——修改行为时务必同步更新。

## 构建与运行

**本地整栈一键起(推荐)**——`scripts/dev-local.sh` 固化了所有 env/端口/启动顺序坑,详见 `docs/08-本地运行.md`:

```bash
scripts/dev-local.sh up          # 起 8 个后端 app(基础设施检查 → staggered + 健康检查)
scripts/dev-local.sh frontend    # 起前端 Vite(自动指向本机网关端口)→ http://localhost:5173
scripts/dev-local.sh status      # 各服务健康表    down/restart/logs <svc> 同名子命令
# 本机端口冲突(8080-8090/5432/6379 被占)→ 覆盖写进 scripts/dev-local.env(见 .env.example;已 gitignore)。
# 脚本内建规避的四个坑:必须 bash 非 zsh;网关也要 RECSYS_SECURITY_ENABLED=false(否则 /api/advertiser 等 401);
# rec-engine/advertiser/ad-serving 都要 PG_JDBC_URL+PG_DS1_JDBC_URL(ad 读侧 ShardingSphere,否则健康 DOWN/广告标题空);staggered 启动避免 pg 连接池风暴。
```

手动/单服务:

```bash
# JDK 21（pom 中设置 java.version=21）
export JAVA_HOME=$(/usr/libexec/java_home -v 21)

mvn clean install                          # 构建所有模块
mvn -pl recsys-common install              # 构建单个模块（加 -am 则连同其依赖）
mvn -pl recsys-rec-engine spring-boot:run  # 运行某个服务（仅限 [app] 模块）
mvn -pl <module> test                      # 测试单个模块
mvn -pl <module> -Dtest=ClassName#method test   # 运行单个测试

docker compose up -d                       # postgres(pgvector) + redis；首次启动时自动应用 schema
docker compose --profile full up -d        # 同时启动 kafka + nacos（可选组件）
docker compose --profile obs up -d         # prometheus + grafana + alertmanager（观测；Grafana localhost:3001）

# 容器化 [app]（E4）：一个参数化 Dockerfile 服务所有 Spring Boot app（recsys-streaming 除外，需 --add-opens）
docker build --build-arg MODULE=recsys-rec-engine --build-arg PORT=8081 -t recsys/rec-engine .
# k8s 部署模板见 deploy/k8s/；readiness 探针 /actuator/health/readiness 含 ModelReadinessHealthIndicator
# （模型策略但 ONNX 没加载 → readiness DOWN，k8s 不导流）。

cp .env.example .env                        # 然后填入 GEMINI_API_KEY 等（.env 已被 gitignore——切勿提交）
```

**离线任务** 不是独立的 main——`recsys-offline` 是一个 Spring Boot 应用，其 `JobRunner` 根据 `--job=<name>` 参数进行分发（每个任务是一个以其 `name()` 为键的 `OfflineJob` bean）：

```bash
mvn -pl recsys-offline spring-boot:run -Dspring-boot.run.arguments=--job=import-items
```

任务名（大致按此顺序运行以引导初始化各存储）：`import-items`、`import-behavior`、`backfill-embedding`、`user-embedding`、`item-cf`、`user-cf`、`swing`、`hot`、`idf`（**R8 词项 IDF**：从 item 标题/类目按与在线 query 理解同源分词 `QueryTokens` 统计 document frequency → Redis `idf:terms`/`idf:doc-count`，供在线给 `TermWeight` 赋 IDF 权重；`--min-df`）、`build-features`、`gen-samples`、`gen-samples-mt`（多任务 + 行为序列样本 → `train/samples_mt.csv`，一个喂给 MMoE/DIN 的**独立**文件；见下文）、`import-tower`（从 `train/item_tower.csv` 加载双塔 item 向量到 `item_tower_embedding`；会自建该表）、`import-semantic-id`（从 `train/item_semantic_id.csv` 加载 RQ-VAE 语义 ID 到 `item_semantic_id`，供生成式召回；会自建该表，先跑 `train_rqvae.py`）、`gen-samples-impr`（**曝光日志闭环**：从真实 `IMPRESSION` 日志 + 后续正反馈归因构造 `samples_mt.csv`，得真实负样本 + 真实位次喂 PAL；as-of 无穿越；与 `gen-samples-mt` 同契约、择一使用）、`sim-rec-events`（**教学用**：造带位置偏置的曝光+点击写 `user_behavior`，让闭环 + PAL 可演示；`--clear`/`--sessions`/`--slate`/`--decay`）、`bandit-stats`（**R7 全量 contextual bandit 充分统计**：从 `user_behavior` IMPRESSION(上下文)+ 正反馈(reward)经 `FeatureService`+`FeatureAssembler`(与在线同源)重建排序稠密上下文 `x`，累积单共享岭回归 `A=λI+Σxxᵀ`/`b=Σr·x` 写 Redis `bandit:model`(JSON)，供在线 `BanditScorer` 出 LinUCB/Thompson 探索加成；`--incremental` warm-start、`--days`/`--lambda`/`--min-rating`/`--max-ts`；纯数学核 `LinUcbModel`）、`data-quality`（**E6 数据质量/漂移巡检**：embedding 覆盖率 + pCTR 校准偏差 ECE(从 `ad_event` 分桶比对实际 CTR) + 类目分布 PSI(近 N 天 vs 全历史);越阈值 WARN + 写 `eval/data-quality-<ts>.csv`,纯函数 `DataQuality`;`--min-embedding-coverage`/`--max-ece`/`--max-psi`/`--days`）、`lookalike`（**A3 智能定向 Look-alike**:从 `ad_audience_seed` 种子用户的 `user_embedding` 求质心 → pgvector ANN 扩散取 top-N 相似用户 → 物化人群包 Redis set `ad:audience:{id}`,供在线广告按 `ad.audience_id` 做 SISMEMBER 定向;`--top-n`/`--audience`）、`run-dag`（**E5 作业 DAG 调度**：按声明式依赖拓扑序串行执行一组作业,**断一环即止**(下游标 skipped) + Redis 分布式锁防并发 + `job:status:{name}` 状态;`--pipeline=recall|ranking|full|quality` 或 `--targets=a,b`(自动补齐传递依赖) + `--dry-run`,纯函数 `Dag`)。不带 `--job` 运行时会打印可用任务集并退出。**评估任务**（在各存储构建完成后运行）：`eval`（离线推荐质量——按时间切分的留出正样本 → 复用在线 recall→rank 流水线 → Precision/Recall/NDCG/MAP/MRR/HitRate/Coverage/Diversity/Novelty @K；支持 `--k=10,20,50`、`--recall-only`（逐通道召回）、`--rank-strategy=v1|onnx|deepfm`、`--max-users`、`--threads`（逐用户并行评估，默认 = CPU 核数）；写出 `eval/metrics-<ts>.csv`；`--print-split` 只打印时间切分点 splitTs 供严格 eval 用）和 `ab-report`（基于 `IMPRESSION`/点击曝光日志的在线逐桶 CTR **+ 统计显著性(P2)**：相对基线桶做 Wilson 95% 置信区间 + 两比例 z 检验 → p 值 / 是否显著(`--alpha` 默认 0.05) / lift / 检测该 lift 的每臂最小样本量，纯函数 `AbStats`；**AA 校验**=把同策略两桶当 A/B 跑，显著即分桶/埋点有偏；`--baseline`/`--alpha`/`--power`；写出 `eval/ab-report-<ts>.csv`）。**严格 eval**（消除乐观偏置，docs/04 §9 #4）：行为派生作业 `item-cf`/`user-cf`/`swing`/`hot`/`user-embedding` 支持 `--max-ts=<epochSeconds>`，只用切分点前的行为聚合(共享 `BehaviorQuery`，口径同 `AsOfFeatureBuilder`)；`bash recsys-offline/run_strict_eval.sh` 一键完成"取 splitTs→`--max-ts` 重建无泄漏存储→eval→自动用全量重建恢复"(实测 pipeline 偏置仅 ±1~2%;TWO_TOWER/SEMANTIC 伪 query 为残余,见 docs/04 §9 #4)。**搜索广告作业**（docs/05，需先建 `02_ad_schema.sql`）：`seed-ads`（造 mock 广告主/广告/竞价词，从现有电影 item 复用创意与 `item_embedding`，并写 Redis 竞价词倒排 `bidword:inv:{keyword}`；**每广告造 `--creatives-per-ad`(默认 3)套创意标题变体进 `ad_creative` 供 DCO**；`--clear` 重置；`--ads`/`--advertisers`/`--seed`）、`sim-ad-events`（**教学用**广告曝光/点击模拟，刻意制造"pctr 系统性高估"给校准喂数据；**并把事件铺在过去 `--days` 天、转化时刻=点击时刻+指数延迟(`--conv-delay-days`),晚于 now 的转化即右删失,复现延迟反馈偏差**；非真实流量）、`gen-ad-cvr-samples`（**A6 延迟反馈 DFM 样本**：从 `ad_event` CLICK↔CONVERSION(join `request_id+ad_id`,`ts<=now()` 右删失)造带删失标签的广告点击 CVR 样本——每 CLICK 一行 `converted/delay_days/elapsed_days` + 经 `ad.item_id`(分片 `ad` 表解析)用 `FeatureService`+`FeatureAssembler`(与在线 `DfmCvrService` 同源)装配的特征 → `train/ad_cvr_samples.csv`,喂 `train_dfm.py`；`--days`/`--seed`）、`ad-calibrate`（用 `ad_event` 拟合 pCTR 保序回归 PAVA → Redis `ad:calib:{model}`；`--model`/`--bins`）、`ad-report`（按广告位次聚合曝光/点击/转化/收入/eCPM/CTR/CVR/相关性，写 `eval/ad-report-<ts>.csv`）、`ad-delay`（**延迟转化建模**：把已到达转化的延迟 `转化时刻−点击时刻` 拟合成指数分布 MLE `λ=1/mean` → Redis `ad:delay:lambda`，完成曲线 `c(e)=1−e^(−λe)`；供 `ad-ocpc` 纠偏；`--days`/`--min-samples`/`--max-delay-days`；docs/05 §6）、`ad-ocpc`（**oCPC 反馈控制**：用 `ad_event` 算各广告主"实际 CPA"，`k_new=clamp(k_old·(目标CPA/实际CPA)^alpha, min, max)` → Redis `ad:ocpc:{adv}`，在线 `OcpcBidder` 出价 `bid=targetCpa·pCVR·k`；**`--delay-correct`(默认开)用 `ad-delay` 的 λ 对每个已到达转化按 `1/c(elapsed)` 做 Horvitz–Thompson 加权,补回在途转化得无偏终值再算 CPA,根治延迟反馈导致的 CPA 高估/k 过度下调**；`--days`/`--alpha`/`--min`/`--max`/`--delay-correct`/`--min-completion`）、`ad-explore-stats`（**新广告 EE** + **DCO 创意级统计**：聚合 `ad_event` 每广告曝光/点击 → Redis `ad:stats:{adId}`+`ad:stats:total`（在线 `ExplorationService` 算 UCB 探索加成）；并按 (广告,创意) 聚合 → `ad:cstats:{adId}:{creativeId}`（DCO 的 `CreativeSelector` 读，docs/05 §7 M7））、`ad-quality`（**质量度精细化**，docs/05 §7 M7：从 `ad_event` 聚合相关性/经验 CTR/CVR 三因子，纯函数 `QualityScore` 贝叶斯收缩 + 归一融合成围绕 1.0 的乘子 → Redis `ad:quality:{adId}`，替代随机基线 `quality_score`；明细写 `eval/ad-quality-<ts>.csv` 供审计；`--days`/`--min-impr`/`--w-rel`/`--w-ctr`/`--w-cvr`/`--q-min`/`--q-max`/`--prior-impr`/`--prior-click`；在线 `QualityScoreService` 查表缺失退基线）、`ad-attribution`（**多触点归因 MTA**，A5：从 `ad_event` 按 `user_id` 重建转化路径，回看窗口内触点（CLICK=点击后归因 CTA / IMPRESSION=浏览后归因 VTA，同广告折叠 CLICK 优先），用纯函数 `MultiTouchAttribution` 三模型（`linear`/`position` U 形/`time-decay` 2^(−age/halfLife)，每次转化恰分出 1.0 信用）把信用摊到各触点，写 `eval/ad-attribution-<ts>.csv`（last-touch vs MTA 信用 + CTA/VTA 拆分 + `credit_delta`）替代单一 last-touch；纯统计报表，不写在线/不进计费；`--model`/`--days`/`--half-life-days`/`--click-window-days`/`--view-window-days`/`--first`/`--last`）。

**排序模型（`recsys-offline/train/`）**——两个训练器喂给在线 ONNX 链路，由 `recsys.rank.strategy`（`RANK_STRATEGY` 环境变量）选择，二者在模型缺失/失败时均回退到规则打分：
- `train_lgbm.py` → `model.onnx`（LightGBM，`strategy=onnx`）：单一稠密输入 `[N,5]`（即 5 个 `FeatureAssembler` 特征）。
- `train_deepfm.py` → `model_deepfm.onnx` + `rank_schema.json` + `category_vocab.json`（PyTorch DeepFM，`strategy=deepfm`）：**双输入** `dense[N,5]` float + `sparse[N,3]` int64（userId/itemId/category embedding）。在线 `DeepFmRankService` 通过 `SparseFeatureEncoder` 编码稀疏 id，其分桶（`floorMod`）+ category 词表必须与训练器完全一致——这是 embedding 的在线/离线契约，类比于稠密特征的 `FeatureAssembler`。**两个训练器共用一个 `samples.csv`**（gen-samples 输出稠密特征 + 原始 `user_id,item_id,category`）；`train_lgbm` 会忽略原始 id 列。**gen-samples 默认按 as-of（时间点）方式计算特征**，通过 `AsOfFeatureBuilder` 实现——它按 ts 顺序流式处理 ratings，并严格在应用该事件**之前**对每个样本的特征做快照，因此不会有未来/标签信息泄漏到训练中（无需 Redis 的 `feat:*`）。传入 `--leaky` 则改为读取全历史 `feat:*` 聚合（旧行为，仅为评估对比“修复泄漏前/后”的 A/B 而保留；需先运行 `build-features`）。DeepFM 使用**随机**训练/验证切分（id-embedding 模型需要训练集见过每个 id），因此其 AUC 不能与 LightGBM 的时间切分 AUC 比较——请用 `eval` 任务做公平的排序对比。DeepFM 需要 `pip install -r requirements-deepfm.txt`（torch + onnxscript）；导出器被强制为单个自包含的 `.onnx`（无外部 `.data`），因为 Java 从 classpath 以字节数组方式加载它。重新训练后，执行 **`mvn -pl recsys-rank clean install`** 以将模型重新打包进 jar（单纯 `install` 会在 `target/classes` 中留下陈旧资源）。
- `train_dcn.py` → `model_dcn.onnx` + `dcn_schema.json` + `dcn_category_vocab.json`（PyTorch **DCN v2**，`strategy=dcn`，R5）：**与 DeepFM 完全相同的在线契约**——双输入 `dense[N,5]` + `sparse[N,3]`、输出 `ctr[N,1]`、复用同一 `SparseFeatureEncoder`/`FeatureAssembler`(稀疏编码常量 USER/ITEM_BUCKETS 与 DeepFM 同源、逐位一致)。模型体是 **Cross Network(v2 矩阵形式 `x_{l+1}=x0⊙(W_l·x_l+b_l)+x_l`,L 层=到 L+1 阶显式交叉)并联 Deep MLP**,拼接后出 CTR——这是它相对 DeepFM(FM 二阶)的净增量:显式、可控阶数的高阶交叉。在线 `DcnRankService` 是 `DeepFmRankService` 的镜像,只换模型/schema/vocab 路径与 `strategy=dcn`,线上编码器/取数零改动;随机切分、IR9/自包含 `.onnx`、模型缺失回退规则均与 DeepFM 同。`eval --rank-strategy=dcn`;依赖同 `requirements-deepfm.txt`。
- `train_dfm.py` → `model_dfm_cvr.onnx` + `dfm_cvr_schema.json` + `dfm_cvr_category_vocab.json`（PyTorch **延迟反馈 DFM ad-CVR**,Chapelle 2014,A6）：**不是 rank 策略**,是**广告专用 CVR 模型**,导出到 **`recsys-ad`**(不是 recsys-rank)、由 `recsys-ad` 的 `DfmCvrService` 在线服务。共享 embedding+DNN body → 两头(CVR 头 `p=sigmoid` + 延迟头 `λ=softplus`),**带删失联合训练**(converted:`−log p−log λ+λd`;censored:`−log((1−p)+p·e^(−λe))`)根治延迟转化删失偏置,**只导出 CVR 头**(去偏 pCVR)。在线契约与 DeepFM 完全一致(dense[N,5]+sparse[N,3]→单输出 pcvr[N,1]、同 SparseFeatureEncoder、IR9 单文件),故 `DfmCvrService` 是 `DeepFmRankService` 镜像(返回 itemId→pCVR map)。样本来自 `gen-ad-cvr-samples`;`SearchAdsOrchestrator` 在 `recsys.ad.cvr.enabled` 且模型就绪时用它覆盖复用 MMoE 头的 pCVR,缺模型退回(零降级)。
- `train_mmoe.py` → `model_mmoe.onnx` + `mmoe_schema.json` + `mmoe_category_vocab.json`（PyTorch **MMoE 多任务 + ESMM**，`strategy=mmoe`）：与 DeepFM 相同的双输入，但**双输出** `ctr[N,1]` + `cvr[N,1]`。在 `samples_mt.csv`（不是 `samples.csv`）上训练，该文件由 `gen-samples-mt` 输出，带**两个标签**——`label_click`（是否有过任何交互）和 `label_like`（评分 ≥4）；负样本是按热度采样的未交互项。ESMM 损失 = `BCE(pCTR, click) + BCE(pCTR·pCVR, like)`，在整个空间上计算（CVR 塔从不直接看到“曝光未点击”，从而消除 CVR 的样本选择偏差）。在线 `MmoeRankService` 读取两个 head 并融合为 `score = pCTR·(cvrBias + cvrWeight·pCVR)`（权重在 `recsys.rank.multi-task` 中，无需重训即可调；`cvrBias=0,cvrWeight=1` ⇒ pCTCVR）。
- `train_din.py` → `model_din.onnx` + `din_schema.json` + `din_category_vocab.json`（PyTorch **DIN 行为序列 + MMoE head**，`strategy=din`）：**四输入** `dense[N,5]` + `sparse[N,3]` + `seq[N,L]`（item 分桶）+ `seq_len[N]`，双输出 ctr/cvr。候选 item 对用户历史做 target-attention（候选与序列**共享 item embedding 表**），padding 位置通过 `seq_len` 掩码（空序列 ⇒ pooled 向量强制为 0，因此冷用户不会得到垃圾值）。`gen-samples-mt` 为每个样本输出 as-of 行为序列（最近 ≤20 个评分 ≥4 的 item，在当前事件**之前**做快照 = 时间点快照，无泄漏）。在线 `DinRankService` 在请求时**通过 JdbcTemplate** 拉取用户最近 ≤`seqLen` 个评分 ≥4 的 item（每个用户查询一次，在候选间广播），由 `SequenceEncoder` 编码，其固定长度 + 右侧 padding + item 分桶必须与 `train_din.py` 一致（序列的在线/离线契约，类比于 `SparseFeatureEncoder`）。MMoE 与 DIN 二者：随机切分、双输出 ONNX、与 DeepFM 相同的 IR9 / 自包含 `.onnx` 注意事项；二者在模型缺失时均回退到规则打分。`eval` 任务接受 `--rank-strategy=mmoe|din`。注意评估的 ground-truth（评分 ≥4 的留出正样本）**等同于 DeepFM 的单一标签**，因此 DeepFM 在 NDCG/precision 上领先，而多任务模型在 HitRate@20 / coverage / diversity / novelty 以及离线 AUC 上领先——这是真正的多目标权衡，而非退化。`run_eval_compare.sh` 在同一 ground truth 上运行全部五种策略。**位置偏差去偏(PAL,docs/04 §13)**:`train_mmoe.py`/`train_din.py` 的 CTR head 训练时叠加位置偏置塔 `b(position)`(吃 `gen-samples-mt` 多输出的 `position` 列),**导出 ONNX 时 `position=None` 该塔不入图** —— 线上输入/编码器/契约一概不变(PAL 相对"位置当特征+预测置0"的关键优势:在线零改动)。`position=0`=位次未知(`gen-samples-mt` 评分派生样本无真实曝光位次 → 默认全 0,PAL 休眠、精度等同未去偏)。**真实位次走曝光日志闭环**(docs/04 §15):`ExposureLogger` 记 `user_behavior.position`,`gen-samples-impr` 从曝光日志造样本(真负样本+真位次)→ PAL 激活(已本地实测:relevance 头 AUC 0.44→0.56、导出仍 dense+sparse 双输入);`sim-rec-events` 为教学模拟器;`gen-samples-mt --position-proxy` 则用热度名次代理(仅教学)。
- `train_dien.py` → `model_dien.onnx` + `dien_schema.json` + `dien_category_vocab.json`（PyTorch **DIEN 兴趣演化**，`strategy=dien`，R1）：在 DIN 之上加**兴趣抽取层(GRUCell)+ 兴趣演化层(AUGRU,注意力门控更新门)**——时间步按固定 `SEQ_LEN` 展开导出静态 ONNX(**必须 `dynamo=False`**,否则 `.chunk→Split` 带 opset18 的 `num_outputs` 使 onnxruntime 加载失败)。**在线契约与 DIN 完全一致**(同 4 输入 dense/sparse/seq/seq_len + 2 输出 ctr/cvr、同 `SparseFeatureEncoder`/`SequenceEncoder`、同 R2 的 `rt:user:seq` 序列来源),故在线 `DienRankService` 是 `DinRankService` 的镜像,只换模型文件与 `strategy=dien`,线上 encoder/取数零改动。模型缺失回退规则;`eval --rank-strategy=dien`。
- `train_sim.py` → `model_sim.onnx` + `sim_schema.json` + `sim_category_vocab.json`（**SIM 长序列**，`strategy=sim`，R3）：两阶段 = **GSU 类目硬检索**(`SimGsu`:从用户近 `sim-long-history`=500 条正反馈长历史里,按<b>每个候选</b>的类目检索最近 k 个同类目 item,每候选各自子序列)+ **ESU**(DIN 架构模型对 GSU 子序列做注意力)。突破 DIN 只吃近 ≤20 的限制,用一次长历史查询 + 内存过滤把有效历史拉长一个量级。ESU 与 DIN 同架构/契约,故 `train_sim.py` 直接复用 DIN(输出改 sim_*);在线 `SimRankService` 逐候选跑 GSU→ESU,模型缺失回退规则。**双塔粗排(R6)**:`PreRankService` 的 `pre-rank.mode=two-tower` 时,经 `TowerScorer`(由 `TwoTowerRecaller` 实现,复用 user 塔 + `item_tower_embedding` 打候选双塔点积)把归一化学习分叠加进线性粗排分,缺向量/未就绪退纯线性。

**召回模型（`train/train_two_tower.py`）**——一个纯 ID 的双塔 / DSSM，用于*学习型*召回（通道 `TWO_TOWER`），与基于内容的 `VECTOR` 通道互补。在 `samples.csv` 中的正向 `(user_id, item_id)` 对上训练（仅用 label/id/category 列，因此 as-of 与 leaky 无关），采用批内采样 softmax。输出：(1) `item_tower.csv`——每个 item 的 64 维向量（item 塔 = itemId+category embedding），由 `import-tower` 任务加载到 `item_tower_embedding`；(2) `user_tower.onnx` + `tower_schema.json`，放入 **`recsys-recall`** 的资源（不是 rank 的）——即 user 塔（userId-bucket embedding）。在线 `TwoTowerRecaller` 加载 `user_tower.onnx`，为 `user_bucket = floorMod(userId, user_buckets)` 计算查询向量（与训练相同的取模；`user_buckets` 来自 schema），然后在 `item_tower_embedding` 上做 pgvector 余弦 ANN。item 向量在离线烘焙好，因此在线无需 item/category 词表。与 DeepFM 相同的 ONNX 导出注意事项（IR9，单个自包含文件）。重新训练后，执行 **`mvn -pl recsys-recall clean install`** 以重新打包 `user_tower.onnx`。优雅降级：模型缺失 → 通道返回空，其他通道兜底。逐通道 `eval --recall-only` 显示 `TWO_TOWER` 在学习型通道中居首（注意：在含测试正样本的整个周期上训练，因此绝对数值偏乐观——与 CF/vector 通道相同的注意事项）。

**生成式召回模型（`train/train_rqvae.py`，docs/04 §14）**——TIGER 范式的可服务版，通道 `GENERATIVE`。**RQ-VAE**（残差量化自编码器）读 item 向量（默认 `item_tower.csv` 双塔 64 维，`--input` 可换内容 embedding）→ 3 层残差量化（每层 K=256 codebook，straight-through）→ 每个 item 得语义 ID `(c0,c1,c2)` 写 `train/item_semantic_id.csv`，由 `import-semantic-id` 任务灌入 `item_semantic_id` 表（需先建 `03_semantic_id.sql` 或由任务自建）。在线 `SemanticIdRecaller`（`recsys-recall`）取用户近期正反馈 item 作种子 → 查语义 ID → 按**最长公共前缀深度**（c0/c0c1/c0c1c2）检索同簇 item。**为什么是前缀检索而非自回归生成**：完整 TIGER 在线要跑自回归 Transformer + beam search，Java serving 复杂；本版退化为“按语义 ID 前缀找同簇”，零额外在线模型、可降级。LEVELS 固定 3（与表 c0/c1/c2 + recaller 对齐）。优雅降级：表缺失/无种子 → 通道空，其他路兜底。

数据库 schema 位于 `recsys-offline/sql/01_schema.sql`，由 postgres 容器在首次启动时自动执行（挂载到 `docker-entrypoint-initdb.d`）。若要重新执行，删除 `pgdata` 卷。

> 注意：仓库当前没有自动化测试；上面的 `mvn ... test` 行是添加测试时应遵循的约定。

## 架构

**模块布局**——`[app]` = 可运行服务（端口），`[lib]` = 计算/领域库（无可执行 jar）：

- `recsys-common` — 共享契约：接口、DTO（record）、常量、Redis key。**并行开发的基础；被一切所依赖。改动它会波及所有地方——编辑前先广播。**
- `recsys-rec-engine` [app :8081] — 编排，主要的对外入口。`GET /api/recommend`（链路含**双塔粗排 R6**、**影子流量 P5**——`recsys.shadow.*` 按比例异步跑影子排序策略只打点不影响返回、**readiness 探针 E4**）；**实验管理面 P3**：`/api/experiment`（GET 快照 / POST 开关·放量·停止 / DELETE 清空，写 Redis `recsys:exp` 覆盖层，改实验不重启）；通过 `GET/POST /api/user/{id}/interests` 进行冷启动兴趣引导；`GET /api/search-ads`（搜索广告：`SearchAdsOrchestrator` 串 query 理解→广告召回→相关性门槛→预算过滤→复用排序模型出 pCTR/pCVR→校准→**oCPC 自动出价**→eCPM 竞价→GSP 计费→曝光埋点）+ `POST /api/ad/click`（CPC 点击计费,带**反作弊**:`AntiFraudService` 去重/频次 + 归因校验,无效点击落 `INVALID_CLICK` 不计费）+ `POST /api/ad/conversion`（转化回传）+ `GET /api/feed`（**混排信息流**：`FeedOrchestrator` 取自然推荐 + 广告→频控过滤→`AdMixer` 按 Ad Load 位次/密度混排→带"赞助"标记返回，docs/05 §4.8）。
- `recsys-gateway` [app :8080] — Spring Cloud Gateway 路由。**限流(E7)**:`RequestRateLimiter`(Redis 令牌桶,`RateLimitConfig.userKeyResolver` 按 userId→IP→global 分桶),是 **opt-in `ratelimit` profile**(`--spring.profiles.active=ratelimit`,需 Redis;`GATEWAY_RATE_LIMIT_RPS`/`_BURST` 调参),默认关、无 Redis 也能起。
- `recsys-behavior` [app :8082] — 行为接入。`POST /api/behavior` → Kafka 或 DB。
- **搜推广控制台(前后端分离)** — 前端 + 后端两块:
  - `console/`(仓库根,**独立前端工程**,非 Maven 模块)— React18+TS+Vite+AntD+ECharts 单页应用。dev `cd console && npm run dev`(Vite :5173,`/api` proxy 到网关 :8080);prod 由 **nginx 同源托管静态 + 反代 `/api` 到网关**(`console/Dockerfile` + `default.conf.template`,`docker compose --profile console up`,`RECSYS_GATEWAY` 配网关地址)。**全站经网关 :8080 同源,零 CORS**(前端用相对 `/api`)。三大分区:①在线调试台(推荐/搜索/搜索广告/混排Feed + Query理解/实验管理/冷启动兴趣,可视化召回→排序→竞价→GSP计费)②广告主后台(广告主/广告/创意/竞价词 CRUD + 审核 + 报表)③离线报表(eval/ab-report/ad-report/data-quality/ad-quality → ECharts)。详见 `console/README.md`。
  - `recsys-console` [app :8090] — **控制台后端 console-api**(原 `recsys-web` 重命名而来,前后端分离后不再托管前端)。当前只提供离线报表读取 `GET /api/console/report/{index,file}`(`ReportController`+`ReportService`,`recsys.console.eval-dir` 配 `recsys-offline/eval` 目录,路径穿越防护;Phase 1 后主读 `eval_report` 表)。网关单条 `Path=/api/console/**` → 8090。
- `recsys-offline` [app] — 离线任务：数据导入、CF 批处理、embedding 回填、样本生成。`train/` 中放有 Python 的 LightGBM/DeepFM/two-tower → ONNX 脚本。
- `recsys-streaming` [app] — Flink 实时特征任务（`RealtimeFeatureJob`），运行在本地嵌入式 MiniCluster 上（无需独立的 Flink 集群）。消费 Kafka `behavior-events` → 实时热门 ZSet `recall:rt_hot` + 每用户实时类目偏好 `rt:user:{id}`（Hash，field=category，value=近期计数，带 TTL）。`HotRecaller` 先读 `recall:rt_hot`，回退到离线的 `recall:hot`。`TagRecaller` 把 `rt:user:{id}` 融合进 TAG 通道——实时类目与静态的 `app_user.profile` 类目取并集，并按近期计数加权（`weight = 1 + boost·count/maxCount`），这样用户*当下*正在互动的内容即使其 profile 尚未包含也能在 TAG 中浮现（开关 `recsys.recall.tag.realtime-enabled`，默认开启；Redis 不可用时降级为仅静态）。构建 fat jar（`mvn -pl recsys-streaming -am package`）并通过 `bash recsys-streaming/run-streaming.sh` 运行（该脚本添加了 Flink 所需的 Java 21 `--add-opens` 标志）。需要 `docker compose --profile full up -d`（Kafka）以及以 `BEHAVIOR_USE_KAFKA=true` 运行的 behavior。
- `recsys-ad` [lib] — 搜索广告库(docs/05,M4–M7):`AdRecallService`(query→ad 关键词倒排/语义/**U2A 用户向量定向**(`user_embedding`→`ad_embedding` 余弦 ANN,query 无关的个性化补充,新用户无向量则空)/兜底多路召回)、`RelevanceGate`(相关性门槛;U2A 以 user↔ad 余弦作相关性依据,同 SEMANTIC_AD)、`OcpcBidder`(**oCPC 智能出价**:`bid=targetCpa·pCVR·k`,k 读 Redis `ad:ocpc:{adv}`,缺则退手动出价/k=1)、`BiddingService`(eCPM 竞价 + GSP 次价拍卖计费;**多计费模式(A1)**——计费因子 `billFactor` 按 `BidType`(CPC/OCPC/CPM/OCPM/CPA,读 `ad.optimization_type`)变:CPC/OCPC=pCTR·q·rel、CPM=q·rel、CPA/OCPM=pCTR·pCVR·q·rel,使 `eCPM=pacedBid·billFactor` 跨计费单位同尺可比、GSP `charged=nextRank/billFactor` 天然按自身单位计价(VCG/Listwise 一并正确);结算按事件分流,编排层按 `SponsoredAd.bidType`:CPM/OCPM 曝光扣、CPC/OCPC 点击扣、CPA 转化扣;**可选 List-wise 外部性**——开关 `recsys.ad.auction.listwise.enabled`/`AD_LISTWISE_ENABLED`,开启则委托纯函数 `ListwiseExternality` 做整页贪心:候选有效质量按与已选广告相似度 `1−λ·maxSim` 衰减(MMR 思路,相似度由 `AdEmbeddingSimilarity` 复用 `item_embedding` 余弦提供,缺向量退 0),计费走外部性 GSP=次优 adjRank÷自身上下文有效质量;关闭/无相似度 → extFactor 恒 1 → 与逐条 eCPM+原 GSP 完全等价,docs/05 §7 M7;**另可选完整 VCG 位置拍卖**——开关 `recsys.ad.auction.vcg.enabled`/`AD_VCG_ENABLED`(开启时优先于 List-wise/GSP),委托纯函数 `VcgAuction` 按位置点击折扣 θ_j 建模 CTR、付外部性 `T_k=Σ value_{(j)}·(θ_{j-1}−θ_j)` 换算 CPC `price_k=T_k/(θ_k·effQuality_k)`,激励相容;分配按 rankEcpm 占位、计费用不含探索加成的 value,单广告位退化为 GSP、默认关行为不变,docs/05 §4.6/§7 M7)、`AuctionGame`(**博弈稳定性分析器**,纯函数离线诊断、不改在线:分离-CTR 位置拍卖模型下度量激励相容——VCG 真实(`maxTruthfulRegret`≈0)、GSP 非真实(`bestResponse` 暴露顶位压价获利偏离)、GSP 对称纳什均衡复现 VCG 价(`gspSymmetricNashBids`,EOS/Varian 等价)、联合出价套利检测(`findCollusionArbitrage` 证 GSP+VCG 均不防合谋),docs/05 §7 M7)、`IsotonicCalibrator`(pCTR 保序回归校准,读 Redis `ad:calib:{model}`)、`IsotonicCalibrator`(pCTR 保序回归校准,读 Redis `ad:calib:{model}`)、`QualityScoreService`(**精细化质量度**,docs/05 §7 M7:读 Redis `ad:quality:{adId}`(离线 `ad-quality` 算的相关性/CTR/CVR 三因子融合分)替代随机基线 `quality_score` 进 eCPM,缺失退基线;长期性能加权,与 EE 临时探索正交)、`CreativeSelector`+`CreativeBandit`(**DCO 动态创意优化**,docs/05 §7 M7:竞价后对每条竞得广告用 UCB 多臂老虎机按创意级 CTR(`ad:cstats:{adId}:{creativeId}`)从 `ad_creative` 多套创意里择优展示,只换展示创意/不动排序计费,`ad_event.creative_id` 闭环归因;开关 `recsys.ad.dco.enabled` 默认关,无创意退默认)、`PacingService`(实时预算熔断 + pacing 平滑)、`AdEventLogger`(`ad_event` 曝光/点击/转化日志)、`AdMixer`(**混排 Ad Load**:广告按位次/密度混入自然结果 + 去重,纯函数)、`FrequencyCapService`(**频控**:Redis 日维度限同广告/同广告主曝光,docs/05 §4.8)、`ExplorationService`(**新广告 EE 探索**:UCB 加成抬升曝光不足广告的排序 eCPM,只进排序不进计费,守红线;统计由 `ad-explore-stats` 物化,docs/05 §6)、`AntiFraudService`(**反作弊**:点击去重 + 每分钟频次 + 归因校验,无效点击落 `INVALID_CLICK` 不计费,守计费公平,docs/05 §6)、`AudienceTargeting`(**智能定向 A3**:按 Look-alike 人群包 `ad:audience:{id}` SISMEMBER 过滤候选,广告 `audience_id` 空=不定向全体可见,离线 `lookalike` 作业扩散,Redis 挂 fail-open)、`GuaranteedDeliveryService`(**品牌 GD 保量 A4**:`ad_contract` 合约按投放进度 `GdPacing` 挑最落后合约置广告位首位、竞价让位、曝光 INCR 交付,开关 `recsys.ad.gd.enabled` 默认关)。`DfmCvrService`(**A6 延迟反馈 DFM ad-CVR**:加载 `model_dfm_cvr.onnx`(Chapelle 2014 带删失联合训练的去偏 pCVR,`train_dfm.py` 产出),`DeepFmRankService` 镜像;`SearchAdsOrchestrator.score()` 在 `recsys.ad.cvr.enabled` 且就绪时用它覆盖复用 MMoE 头的有删失偏置 pCVR,缺模型/未就绪退回 MMoE 头。默认关)。pCTR/pCVR **默认复用** `recsys-rank` 的排序模型(经 rec-engine 编排注入;pCVR 可切 A6 的 DFM),广告只补"query / 钱 / 校准 / 拍卖 / 智能出价"几块净增量。每层优雅降级(关键词路兜底、SEMANTIC_AD 无向量则空、校准缺失退 identity、Redis 挂则不限预算、oCPC 缺参数/系数退 CPC)。**分库分表读侧(docs/05 §8.1)**:`AdShardingConfig` 装配**双数据源**——主(`@Primary` 普通 Postgres,承载召回/排序/embedding/pgvector ANN/`ad_event`,零改动)+ 次(`adShardingJdbc`,ShardingSphere)只读分片广告表;`AdRepository` 把"向量 ANN(主库 `ad_embedding` 不分片全量)+ 广告详情(分片库 `ad⋈advertiser` 绑定表 + `bidword` 出价分查)"拆两步,`PacingService`/`CreativeSelector`/`AdEventLogger` 的广告表读均走分片库——这样 pgvector `<=>` 不进 ShardingSphere 解析路径、又能读到 ds_0/ds_1 两库的广告。
- `recsys-advertiser` [app :8083] — 广告主侧管理服务(docs/05 §2,M8):广告主/广告/创意/竞价词的增删改查 + 投放报表,是广告的**写侧/管理面**(与 `recsys-ad` 在线检索/竞价/计费互补)。全部端点收在 `/api/advertiser/**` 命名空间(避免与 rec-engine 的 `/api/ad/click`、`/api/ad/conversion` 在网关冲突,网关单条 `Path=/api/advertiser/**` → 8083)。`AdvertiserController`(`/api/advertiser` CRUD + `/{id}/report` 从 `ad_event` 聚合曝光/点击/转化/花费/CTR/CVR/eCPM)、`AdController`(`/api/advertiser/{id}/ad` 建/列、`/api/advertiser/ad/{adId}` 取/改/删 + `/status` 上下线)、`BidwordController`(`/api/advertiser/ad/{adId}/bidword` + `/api/advertiser/bidword/{id}`)、`CreativeController`(`/api/advertiser/ad/{adId}/creative` + `/api/advertiser/creative/{id}`,DCO 多创意)。**创意审核状态机(A2)**:`ad`/`ad_creative` 有 `review_status`(approved/pending_review/rejected,默认 approved 保历史广告);新建广告先过 `CreativeReview` 机审(违禁词/空标题→rejected,否则 pending_review 待人审)→ `POST /api/advertiser/ad/{id}/review?decision=approve|reject` 人审 / `/submit-review` 重送审;**在线只服务 approved**——三处闸门:`AdIndexSyncService.servable`(倒排"可服务集")+ `recsys-ad` `AdRepository.activeAdDetails`(DB 召回路)+ `CreativeSelector`(DCO 只展示 approved 创意)。关键设计:写库的同时 `AdIndexSyncService` **增量维护在线召回所依赖的派生存储**——竞价词倒排 `bidword:inv:{keyword}`(口径同离线 `seed-ads`/`BidwordInvCodec`;在线关键词路不回库校验状态,故倒排即"可服务集":只索引 *广告 active + 审核 approved 且广告主 active* 的竞价词,状态/审核/出价/关键词变化都增量改;广告主级暂停/恢复会 reindex 其全部广告),`ad_embedding`(新建/换关联 item 时从 `item_embedding` 拷贝,供 SEMANTIC_AD/U2A);`AdvertiserView` 的 `spentToday`/`remainingBudget` 读 Redis `ad:budget:{adv}:{yyyymmdd}`(与在线 pacing 同源)。**分库分表(ShardingSphere-JDBC,docs/05 §8)**:本模块的 datasource 走 `jdbc:shardingsphere:classpath:sharding.yaml`(`ShardingSphereDriver`),把广告表分到同容器两个 database `ds_0`(原 `recsys`)/`ds_1`(`recsys_ds1`)——`advertiser` 按 `advertiser_id`、`ad` 按 `advertiser_id`(广告与广告主同库、广告主作用域查询单库命中)、`bidword`/`ad_creative` 按 `ad_id`;`INLINE` 取模算法(`MOD`/`HASH_MOD` 是 auto 算法只配 autoTables)。**主键改用 ShardingSphere 的 Snowflake keyGenerator**(8 字节、时间有序、去中心化;insert 不带主键列,`getGeneratedKeys` 回传)替代单库 IDENTITY(分库后跨库自增冲突);**注意别按 `ad_id` 等 Snowflake 低位取模分片**——低 12 位是序列号、低 QPS 下恒 0 会全压一库,故 `ad` 改按 `advertiser_id` 分。其余表(`item`/`*_embedding`/`ad_event`…)经 `!SINGLE` 默认落 `ds_0`;**`ad_embedding` 刻意不分片**——向量 ANN(hnsw+`<=>`)必须看到全量,分片会漏召回,且 pgvector 非标准算子不进 ShardingSphere 解析路径。跨分片边界的 JOIN 已消除:`report` 拆两查(广告表广播取广告 → `ad_event` 单表按 ad_id 聚合)、`findAdRow`/`listAdRows` 把 `ad_embedding` 标志拆成单表批查。`sharding.yaml` 放在 `recsys-common` 资源里供写侧(本模块,主数据源)与读侧(`recsys-ad`,次数据源)共用。**落库前置**:先跑 `sql/04_ad_sharding.sql`(建 `recsys_ds1` + 分片骨架 + 拆 ds_0 跨库外键/IDENTITY)。**接入进度(全链路已分片)**:写侧(本模块)+ **在线读侧(`rec-engine`/`recsys-ad` 双数据源,docs/05 §8.1)** + **离线侧(`recsys-offline`/`OfflineAdShardingConfig` 双数据源,docs/05 §8.2)** 均已接入并实测——`seed-ads` 写广告表分片落 ds_0/ds_1(实测 advertiser 10/10、ad 100/100),`sim-ad-events`/`ad-ocpc` 读分片库,`ad-report` 等只读 `ad_event`(单表)的分析作业零改动;`/api/search-ads` 召回两库广告。offline 多 arg 用空格分隔(`"-Dspring-boot.run.arguments=--job=seed-ads --clear --ads=200"`,逗号不拆分);ShardingSphere 注意裸聚合(`MAX(x)` 而非 `COALESCE(MAX(x),0)`,否则归并失效每分片各返一行)。优雅降级:Redis 不可用时同步吞异常,在线关键词路回退 DB(仍按状态过滤)。
- `recsys-recall` / `recsys-rank` / `recsys-feature` / `recsys-embedding` / `recsys-content` / `recsys-user` [lib] — 召回、排序、特征、embedding、内容、用户画像库。`recsys-embedding` 有两套 `EmbeddingClient`：`GeminiEmbeddingClient`(默认,联网 REST)与 `LocalBgeEmbeddingClient`(`EMBEDDING_PROVIDER=local`,离线 ONNX——纯 Java `BgeTokenizer`(BERT WordPiece,读 vocab.txt,在线/离线契约同 `SparseFeatureEncoder`)+ onnxruntime 跑 bge-base-en-v1.5(768 维)+ CLS 池化 + L2)。模型由 `recsys-embedding/train/export_bge_onnx.py` 离线导出到文件系统(默认 `~/.recsys/models/...`,体积大不入 git,`BGE_MODEL_PATH`/`BGE_VOCAB_PATH` 可覆盖);缺文件则 `isReady()=false`、抛异常(query 理解 catch 后降级 null)。换 provider = 换向量空间,需全量重灌。`recsys-embedding` 另有生成式 `LlmClient`(契约在 `recsys-common.llm`):`GeminiChatClient`(`recsys.llm.enabled=true` + key 时装配,调 Gemini `generateContent`,Redis 缓存 `llm:cache:`),被 `recsys-query` 经 `ObjectProvider<LlmClient>` 可选注入做 LLM query 理解(拼写纠错/意图/改写,见 docs/04 §11);未就绪则 query 理解走纯词法兜底。

**单体优先，微服务就绪。** `RecEngineApplication` 使用 `@SpringBootApplication(scanBasePackages = "com.recsys")`，因此 `[lib]` 模块的 `@Component`/`@RestController` bean 全部被装配进一个进程。模块边界已经划好，后续拆分为独立服务的成本很低。跨模块耦合应限制在 `recsys-common` 接口上。

**请求流程**（`docs/02-架构设计.md` §6）：rec-engine 检查 `cache:rec:{userId}` → 冷启动检测 + 分层 A/B 分配 → 多通道召回（通道由实验的 recall 变体 / 冷启动覆盖控制）合并去重（主来源按优先级选取；**各路召回分先在 `MultiChannelRecallService` 内按路归一化到 [0,1] 再跨路取 max**——否则热度计数会压垮语义/向量路的余弦分）→ 排序（组装特征 + ONNX 或规则打分，策略按 rank 变体决定）→ 融合 recall+rank 分数（可选叠加近线学习/探索的**加性**信号:FTRL 近线分 `fusion.ftrl` + **R7 全量 contextual bandit 探索加成** `fusion.bandit`——`BanditScorer` 用排序特征 x 出 LinUCB `θ̂ᵀx+α√(xᵀA⁻¹x)` 或 Thompson `θ̃ᵀx`,默认关、缺 `bandit:model` 退 0、`recsys:tuning` 热开关），然后乘以逐通道加成（`recsys.fusion.channel-boost`，默认 `TAG: 1.5`，多命中取最大）以使诸如 TAG 这类兴趣信号——它携带实时 `rt:user` 类目偏好——不被 HOT/CF 热度淹没 → 重排（策略按 rerank 变体决定；冷启动强制强多样性）→ 截断、构建推荐理由、带桶标签记录曝光、缓存、返回。**搜索（query 驱动）场景**用 `recsys.search.*` 覆盖默认融合：抬高召回权重 / 压低排序权重、`channel-boost` 抬升 `SEMANTIC`+`LEXICAL`+`TAG`、`bypass-cold-start`，让 query↔item 相关性主导而非个性化热度（冷用户带 query 也走 query 主导链路）。**混合检索**：搜索请求由编排层注入 `recall-fusion=rrf`，`MultiChannelRecallService` 改用 RRF（`贡献=1/(k+rank)`，`recsys.recall.rrf-k`）按名次融合词法(LEXICAL/BM25)+向量(SEMANTIC)等多路（默认 userId 推荐仍走按路归一化取 max，不受影响）。

**在线与离线的拆分**是核心思想：在线链路是同步、毫秒级延迟的（只做“查表 + 轻量打分”）；离线/近线链路预计算繁重的工作（embedding、CF 倒排索引、训练好的模型）并将结果写入在线存储（pgvector、Redis）。

## 约定与契约（`recsys-common`）

- 核心接口：`RecallService`、`RankService`、`FeatureService`、`EmbeddingClient`。实现位于各自的 `[lib]` 模块中。
- DTO 与 channel/action 类型是 `dto/`、`recall/`、`rank/`、`constant/` 中的 Java `record` / 枚举。
- **所有 Redis key 都通过 `RedisKeys`**——不要把硬编码的 key 字符串散落在各模块中。
- **Embedding 维度固定为 768**（`recsys.embedding.dimension`，与 `item_embedding vector(768)` 匹配）。不同模型维度不同；混用会破坏检索。更换模型意味着对整个语料库重新做 embedding；通过 `model` 列追踪来源。
- 可调参数（召回配额、排序策略、重排上限、缓存 TTL）位于每个 `[app]` 的 `application.yml` 的 `recsys.*` 树下，可通过环境变量覆盖（如 `RANK_STRATEGY`、`EMBEDDING_PROVIDER`）。**Nacos 配置中心(P4,已接 rec-engine)**：rec-engine 引 `spring-cloud-starter-alibaba-nacos-config` + `spring.config.import=optional:nacos:recsys-rec-engine.yml`（`optional:`=Nacos 不可达则退本地 yml 默认，无 Nacos 也能起）+ `refresh-enabled`。把 `recsys.ad.*`（reserve/pacing/频控/ad-load）或 `recsys.fusion.*` 放到 Nacos dataId 即**在线热更不重启**——消费方每请求读活的 `AdProperties` getter，Nacos 推送触发 `ConfigurationPropertiesRebinder` 就地重绑，故**无需给 bean 加 `@RefreshScope`**。与更轻量的 `DynamicTuningService`（Redis 覆盖 `fusion.*`）/`ExperimentOverrideService`（`recsys:exp`）并存。`docker compose --profile full up -d` 起 Nacos（`8848`）。

## 并行开发模型

`PLAN.md` 将第一阶段拆分为相互独立的 Track（A：数据/embedding，B：召回，C：排序/特征，D：Python 训练，E：行为/离线，F：编排/网关/web），每个为一项独立任务。规则：

1. 只改动你所属 Track 拥有的模块。
2. 依赖 `recsys-common` 契约；不要单方面更改它们（先广播）。
3. 对跨 Track 的下游依赖打桩（mock），用 `// TODO` 标注真实来源，并在第二阶段集成时换入真实实现。

代码中不易看出的设计意图：**在线与离线的特征计算必须保持一致**（相同的特征名、相同的逻辑）。特征不一致 / 数据泄漏是部署后推荐质量崩溃的首要原因。每一层都设计为可优雅降级——hot 召回是始终在线的兜底，embedding 回退到本地 BGE/ONNX，排序在 ONNX 模型加载失败时回退到规则打分。**熔断(E7)**:重外呼加 Resilience4j 熔断快速失败到降级——`GeminiEmbeddingClient`(gemini-embedding)、`VectorRecaller` 的 pgvector ANN(pgvector-recall,pg 宕机→VECTOR 路快速返回空、其它路兜底),配置在 rec-engine `application.yml` 的 `resilience4j.circuitbreaker`。
