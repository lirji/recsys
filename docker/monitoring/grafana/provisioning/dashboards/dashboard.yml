# Grafana 启动时自动加载 /var/lib/grafana/dashboards 下的看板 JSON。
apiVersion: 1

providers:
  - name: recsys
    orgId: 1
    folder: recsys
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
