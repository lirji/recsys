# 推荐系统 · 架构设计

> 本文讲**系统总体架构、模块划分、数据模型、服务间契约、请求时序**。核心接口(`RecallService`/`RankService`/`FeatureService`/`EmbeddingClient`)也是并行开发的契约。
> **每个技术点的算法深度见 [`docs/skills/`](skills/README.md);微服务/安全/分库分表的 as-built 细节见 skills/16–18。**
> **as-built 提示**:系统已从"单体优先"演进出可回滚的 **DDD 微服务拆分**(ad-serving/content/user gRPC)。`rec-engine` 的**代码默认**仍是进程内托管一切(`*Gateway` 缝 `in-process` 默认 / `grpc` 可选)——纯本地 `mvn` 单进程跑即单体形态;但**容器全栈(`--profile apps`)默认把三条缝全部切到 `grpc`**,经 Nacos 服务发现(`discovery:///`)调独立服务(见 docker/docker-compose.yml 的 `*_SERVING_MODE`)。

## 1. 推荐系统是怎么工作的(一句话原理)

> 从**海量物品**里,分层漏斗式地筛出**这个用户此刻最可能感兴趣的几个**,展示给他;再把他的真实反馈收回来,让系统越用越准。

经典四段漏斗:

```
百万物品 ──召回──> 几千 ──粗排──> 几百 ──精排──> 几十 ──重排──> 展示给用户
          Recall          Pre-rank        Rank           Re-rank
        (快/广/粗)        (轻量过滤)     (准/重/CTR模型)  (多样性/业务规则)
```

> 学习项目里**粗排可省略**(召回结果不多),先做 **召回 → 精排 → 重排** 三段。

---

## 2. 系统总体架构

```
                         ┌──────────────────────────────────────────┐
                         │        用户 / 前端 console(React SPA)      │
                         └───────────────────┬──────────────────────┘
                                             │ HTTP
                         ┌───────────────────▼──────────────────────┐      ┌─────────────────┐
                         │  API Gateway(边缘认证:自签 JWT 默认 /      │◄────►│ Nacos(默认开)   │
                         │  Casdoor OIDC opt-in;路由 lb://;限流)     │      │ 服务发现+配置中心 │
                         └───────────────────┬──────────────────────┘      └─────────────────┘
                                             │ (内部 HMAC 令牌)
  ┌──────────────────────────────────────────────────────────────────────────┐
  │                        在线服务链路 (低延迟, 同步)                            │
  │                                                                            │
  │   ┌──────────────┐    ┌───────────┐   ┌──────────┐   ┌──────────┐         │
  │   │ rec-engine   │──> │  recall   │──>│  rank    │──>│  rerank  │──> 结果   │
  │   │ (编排/兜底)   │    │ 多路召回   │   │ 特征+模型 │   │多样性/规则│         │
  │   └──────┬───────┘    └─────┬─────┘   └────┬─────┘   └──────────┘         │
  │          │ gRPC(经 Nacos discovery:///,容器全栈默认;in-process 可回滚)      │
  │          ├────────────────┬────────────────┬─────────────────┐             │
  │   ┌──────▼───────┐ ┌──────▼────────┐ ┌─────▼──────────┐      │             │
  │   │ ad-serving   │ │ content-svc   │ │ user-service   │      │             │
  │   │ (广告钱路)    │ │ (item 读模型) │ │ (兴趣画像)      │      │             │
  │   └──────────────┘ └───────────────┘ └────────────────┘      │             │
  └──────────┬──────────────────┬──────────────┬─────────────────┼─────────────┘
             │                  │              │                 │
        ┌────▼──────┐    ┌──────▼──────┐  ┌────▼─────┐   ┌───────▼──────┐
        │  Redis    │    │ pgvector    │  │  Redis   │   │ embedding-svc │
        │(结果缓存) │    │(向量检索)    │  │(在线特征)│   │(Gemini/BGE)   │
        └───────────┘    └─────────────┘  └──────────┘   └──────┬───────┘
                                                                 │
  ┌──────────────────────────────────────────────────────────────────────────┐
  │                   离线 / 近线链路 (异步, 不影响在线延迟)                       │
  │                                                                            │
  │  行为日志 ─Kafka─> 特征计算 ──> 训练样本 ──> LightGBM训练 ──> 导出ONNX        │
  │  物品入库 ──────> embedding ──> 写入 pgvector                               │
  │  行为日志 ──────> ItemCF/Swing 批算 ──> 写入 Redis(i2i 倒排)                │
  └──────────────────────────────────────────────────────────────────────────┘
```

**两条链路的核心区别(重点理解):**
- **在线链路**:用户请求来了实时算,要求毫秒级,只做"查"和"轻打分"。
- **离线/近线链路**:提前把重活算好(向量、协同过滤倒排、模型),把结果"喂"到在线存储里。

> **拓扑补充(as-built)**:①网关边缘认证两模式——Docker 编排默认 Casdoor OIDC RS256 JWKS(`RECSYS_EDGE_CASDOOR=true`,与前端 `VITE_AUTH_MODE=oidc` 成对)，并保留自签 HS256 JWT 作为成对回滚模式(见 docs/09)，通过后签发内部 HMAC 令牌下发;②Nacos 默认开(`NACOS_DISCOVERY:true`),网关路由默认 `lb://`,rec-engine 经 `discovery:///recsys-{ad-serving,content-service,user-service}` 调三个 gRPC 服务,`*_SERVING_MODE=in-process` 可逐条缝回滚单体;③advertiser(写侧)另接统一权限平台 auth-platform/SpiceDB 做细粒度归属判权(`RECSYS_AUTHZ_MODE` 三态,默认 disabled,docs/09)。

---

## 3. 模块划分(Maven 多模块单仓)

```
recsys/                          # 父 POM
├── recsys-common/               # 公共契约:核心接口/DTO/枚举/RedisKeys
├── recsys-ad-common/            # 广告共享契约(BidType/AdCatalogEvent/BidwordInvCodec/sharding.yaml)
├── recsys-proto/                # gRPC 契约(ad_serving/content/user .proto + 防腐 mapper + 拦截器)
├── recsys-platform/             # 平台安全/Web(内部 HMAC token / 过滤器 / 全局异常)
├── recsys-gateway/       [app]  # 网关:边缘认证(自签 HS256 默认 / Casdoor OIDC RS256 opt-in)+ 路由 lb:// + 限流(opt-in)
├── recsys-rec-engine/   [app]  # 推荐编排(对外主入口;/api/recommend|search|search-ads|feed|query|user|ad|experiment;gRPC 客户端)
├── recsys-behavior/     [app]  # 行为采集(埋点 → Kafka/库)
├── recsys-advertiser/   [app]  # 广告主写侧/管理面(CRUD + 审核 + 报表 + 派生存储同步;auth-platform/SpiceDB 归属判权,三态默认关,docs/09)
├── recsys-ad-serving/   [app]  # 广告钱路服务(gRPC 拆分;AdPipeline + 事件驱动 catalog)
├── recsys-content-service/[app] # item 读模型服务(gRPC BatchGetItems)
├── recsys-user-service/ [app]  # 用户兴趣服务(gRPC Get/UpdateInterests)
├── recsys-console/      [app]  # 控制台后端 console-api(原 recsys-web;report/system/user360/…)
├── recsys-streaming/    [app]  # Flink 实时特征(本地 MiniCluster)
├── recsys-offline/      [app]  # 离线作业(40+ Job) + train/ Python 训练脚本 → ONNX
├── recsys-query/        [lib]  # Query 理解(归一/分词/意图/IDF/向量化降级)
├── recsys-recall/       [lib]  # 12 路召回
├── recsys-rank/         [lib]  # 排序(特征装配 + ONNX/规则,9 策略)
├── recsys-ad/           [lib]  # 广告在线库(AdPipeline:召回/相关性/竞价/计费/出价/…/分片读侧)
├── recsys-feature/      [lib]  # 在线特征读写(Redis feat:*)
├── recsys-embedding/    [lib]  # 向量化(Gemini / 本地 BGE ONNX)+ LlmClient
├── recsys-content/      [lib]  # 物品/内容元数据(含 item_local 读 seam)
├── recsys-user/         [lib]  # 用户画像
└── console/                     # 前端 SPA(React18+TS+Vite,非 Maven 模块)
```

> **单体优先、微服务就绪(as-built)**:`rec-engine` 用 `scanBasePackages="com.recsys"` 把 `[lib]` 全装配进一个进程;三个 gRPC 服务(ad-serving/content/user)通过 `*Gateway` 缝按需启用(默认 in-process)。模块边界已划好,拆分成本很低。详见 [skills/16 微服务拆分](skills/16-微服务拆分与gRPC.md)。

---

## 4. 数据模型

### 4.1 PostgreSQL 表

```sql
-- 物品表
CREATE TABLE item (
  item_id      BIGINT PRIMARY KEY,
  title        TEXT,
  category     TEXT,              -- 类型/类目(电影:genres)
  tags         TEXT[],            -- 标签
  description  TEXT,
  popularity   DOUBLE PRECISION DEFAULT 0,  -- 热度(用于热门召回/冷启动)
  created_at   TIMESTAMP DEFAULT now()
);

-- 物品向量表(pgvector)
CREATE EXTENSION IF NOT EXISTS vector;
CREATE TABLE item_embedding (
  item_id   BIGINT PRIMARY KEY REFERENCES item(item_id),
  embedding vector(768),          -- 维度与 EmbeddingClient 配置一致
  model     TEXT                  -- 记录用哪个模型生成,便于换模型
);
CREATE INDEX ON item_embedding USING hnsw (embedding vector_cosine_ops);

-- 用户表 / 用户画像
CREATE TABLE app_user (
  user_id    BIGINT PRIMARY KEY,
  profile    JSONB,               -- 画像:偏好类目、标签权重等
  updated_at TIMESTAMP DEFAULT now()
);

-- 用户向量(由历史行为物品向量聚合而来,用于向量召回)
CREATE TABLE user_embedding (
  user_id   BIGINT PRIMARY KEY,
  embedding vector(768)
);

-- 行为表(点击/播放/评分/收藏...)
CREATE TABLE user_behavior (
  id         BIGSERIAL PRIMARY KEY,
  user_id    BIGINT,
  item_id    BIGINT,
  action     TEXT,                -- impression/click/like/play/rating
  value      DOUBLE PRECISION,    -- 评分值/时长等
  ts         TIMESTAMP DEFAULT now()
);
```

### 4.2 Redis Key 规范

| Key 模式 | 类型 | 内容 | 用途 |
|---|---|---|---|
| `feat:user:{userId}` | Hash | 用户在线特征 | 排序取特征 |
| `feat:item:{itemId}` | Hash | 物品在线特征 | 排序取特征 |
| `recall:hot` | ZSet | 全局热门(score=热度) | 热门召回/冷启动 |
| `i2i:{itemId}` | ZSet | 相似物品(score=相似度) | ItemCF 召回 |
| `u2u:{userId}` | ZSet | 个性化召回列表(离线物化) | UserCF(U2U)召回 |
| `swing:{itemId}` | ZSet | Swing 相似物品(score=相似度) | Swing 召回 |
| `cache:rec:{userId}` | String | 推荐结果(短 TTL) | 结果缓存 |
| `emb:cache:{hash}` | String | 文本→向量缓存 | 省 API 调用 |
| `recall:rt_hot` | ZSet | 实时热门(Flink 滑窗) | 热门召回(优先于离线 hot) |
| `rt:user:{userId}` | Hash | 实时类目偏好(field=类目,value=近期计数,Flink 滑窗) | TAG 召回叠加(实时类目加权) |

> 所有 key 统一经 `RedisKeys` 生成,禁止散落硬编码。

---

## 5. 服务间 API 契约(并行开发的关键!)

> 每个 track 只要遵守这里的输入/输出,就能独立开发、独立 mock 联调。

### 5.1 主入口:获取推荐
```
GET /api/recommend?userId={id}&size={n}&scene={feed}

Response:
{
  "userId": 1,
  "scene": "feed",
  "items": [
    { "itemId": 100, "score": 0.92, "recallFrom": ["vector","i2i"], "reason": "和你看过的X相似" }
  ],
  "traceId": "..."
}
```

### 5.2 召回服务(recsys-recall)
```java
interface RecallService {
    // 多路召回合并后的候选集
    List<RecallItem> recall(RecallContext ctx);
}
// ctx: userId, size, scene, enabledChannels(空=全开,分层A/B/冷启动据此启停), params(如 semantic 的 query)
record RecallItem(long itemId, double recallScore, RecallChannel channel) {}
// RecallChannel ∈ {VECTOR, I2I, HOT, TAG, U2U, SWING, SEMANTIC, COLD,
//                  TWO_TOWER, GENERATIVE, LEXICAL, TIGER}  —— 12 路(见 skills/01)
// 每个 *Recaller 实现 channel() 声明自己所属路;MultiChannelRecallService 并行执行(默认 pool=12,单路超时 150ms;
// 开 Bulkhead 舱壁后按快/慢路拆分线程池隔离,防慢路拖垮快路),
// 逐路归一化后跨路取 max(搜索场景改 RRF);任一路挂不影响整体(HOT 的 DB 兜底永远在)
```

### 5.3 向量化服务(recsys-embedding)
```java
interface EmbeddingClient {
    float[] embedText(String text);
    float[] embedImage(byte[] image);      // 多模态(可选)
    int dimension();                       // 固定维度,如 768
}
// 实现: GeminiEmbeddingClient(默认) / LocalBgeEmbeddingClient(降级)
```

### 5.4 排序服务(recsys-rank)
```java
interface RankService {
    // 输入候选,输出带分数的排序结果
    List<RankedItem> rank(long userId, List<Long> candidateItemIds, String scene);
}
record RankedItem(long itemId, double score, Map<String,Double> featureSnapshot) {}
```

### 5.5 特征服务(recsys-feature)
```java
interface FeatureService {
    Map<String,Double> userFeatures(long userId);
    Map<String,Double> itemFeatures(long itemId);
    // 交叉特征在 rank 内部组合
}
```

### 5.6 行为采集(recsys-behavior)
```
POST /api/behavior
{ "userId":1, "itemId":100, "action":"click", "value":1.0, "ts":... }
→ 写入 Kafka(或直接入库)→ 离线消费
```

---

## 6. 一次推荐请求的完整时序

```
1. 前端 GET /api/recommend?userId=1&size=10
2. rec-engine 查 cache:rec:1,命中直接返回(可选)
3. 判冷(ColdStartDetector)+ 分层 A/B 分桶(ExperimentService):
   得各层 variant → 本次启用的召回路 / 排序策略 / 重排策略;冷用户覆盖 recall/rerank
4. recall:并行多路召回(按 enabledChannels 启停)
   - vector:   user_embedding → pgvector 最近邻        - u2u:    读 u2u:{userId}
   - i2i:      用户近期物品 → i2i:{itemId}              - swing:  用户近期物品 → swing:{itemId}
   - semantic: query/近期标题 → embedding → pgvector    - tag:    画像偏好类目取物品
   - hot:      recall:hot 兜底                          - cold:   跨类目铺开(仅冷启动)
   合并去重(主来源按优先级标记)→ ~几百候选
5. rank:批量取特征(feature)→ 按实验选中的 v1/onnx 打分 → 排序
6. fuse:融合召回分 + 排序分(权重可被 rank 层实验覆盖),再乘召回路 channel-boost(TAG 抬权,见 §7)
7. rerank:RerankRouter 按实验选 diversity/mmr/none(冷启动强制强多样性)
8. 截断 top size,组装 reason;ExposureLogger 异步写曝光(带 bucket);写结果缓存返回
9. 前端展示;用户点击 → POST /api/behavior → 进入反馈闭环;按 bucket 算分桶 CTR
```

---

## 7. 配置与环境

- 维度、模型名、各召回路数量、Redis/PG 连接 → 全部走配置(`application.yml` / Nacos)。
- **融合**:最终分 = `(recall-weight·归一化召回分 + rank-weight·排序分) · channel-boost`。`recsys.fusion.channel-boost`(channel 名大写→乘子,缺省 1.0,多路命中取最大)抬升 TAG 等兴趣信号路,默认 `TAG: 1.5`;设 1.0 即关闭。
- TAG 召回叠加实时类目偏好:`recsys.recall.tag.realtime-enabled`(默认 true)读 `rt:user:{id}`,实时类目按近期计数加权(`realtime-boost`)。
- `GEMINI_API_KEY` 走环境变量,**不进 git**(`.gitignore` 加 `.env`)。
- 容器编排统一在 `docker/docker-compose.yml`(单一入口),按 profile 分层:默认 postgres(pgvector)+ redis;`--profile full` 加 kafka + nacos;`--profile apps` 加 8 个 Java 服务容器(默认接 Nacos 服务发现,nacos 随 apps 一并起);`--profile console` 加前端 nginx;`--profile obs` 加 prometheus/grafana/tempo/alertmanager;`--profile authz` 加 recsys 专属 SpiceDB(:8544,postgres datastore 持久化,docs/09)。一键封装见 `scripts/dev-local.sh`。
