# 搜推广系统 · 优化与扩展路线

> 本文由四维度(算法/ML · 系统架构 · 数据库 · 可靠性)代码级审查汇总而成,按 **影响 × 可行性** 排序。
> 每条均基于**真实代码证据**(非文档声称),标注类型(improve 优化已有 / extend 扩展新能力)与工作量(S/M/L)。
> 进度状态见每条末尾:⬜ 待做 / 🔧 进行中 / ✅ 已落地。

---

## 0. 一句话诊断

**能力密度极高(搜推广全链路:12 召回 + 5 排序 + 广告竞价计费 + 分库分表)、但工程护栏偏薄。** 真正的瓶颈不是模型不够,而是:
1. **召回质量被 HNSW 参数静默腰斩**(`ef_search=40 < LIMIT 200`);
2. **在线无隔离 / 超时 / 熔断**(多路召回串行、外部调用无超时);
3. **排序特征太薄(仅 5 维)+ 无粗排层**——所有模型的共同天花板;
4. **核心链路零测试**,尤其在线/离线特征一致性契约(项目自认"部署即崩溃"首因)。

推进节奏:**先做第一批 5 个快赢 → 再补粗排 + 弹性 + 特征 → 后上测试/CI/去偏**。

---

## 附:文档声称 vs 代码实际(审查核出的偏差)

| 文档声称 | 代码实际 | 证据 |
|---|---|---|
| 多路召回"并行调用" | **串行 `for` 循环** | `MultiChannelRecallService`(注释写"并行",实为单线程) |
| Nacos 配置中心"可选/可热更" | **完全未接入**(无 bootstrap、无 discovery 依赖、无 `@RefreshScope`) | grep 无 `spring.cloud.nacos`;yml 注释仅愿景 |
| 网关"统一入口" | `/api/search`、`/api/feed`、`/api/search-ads`、`/api/ad/*`、`/api/query/*`、`/api/user/*` **未配路由,落到 `/**`→web:8090 不可达** | `recsys-gateway/application.yml` 仅 4 条 route |
| "仓库当前无自动化测试" | 实为**已有 6 个测试,但全是广告纯函数单测**;核心漏斗与契约类仍零覆盖 | `recsys-ad`/`recsys-offline` 下 6 个 test |

---

## 1. 第一批 · 立即可做的「快赢」(全 S,当天可落,高 ROI)

| # | 类型 | 问题(代码证据) | 修法 | 状态 |
|---|---|---|---|---|
| Q1 | improve | **HNSW `ef_search=40` < 召回 `LIMIT 200`**:`item_embedding`/`ad_embedding`/`item_tower_embedding` 三索引全默认参数,`vector/two_tower` 各取 200、`semantic` 取 100,HNSW 只保证约 40 个真近邻,其余为低质量填充,**召回被静默腰斩、无报错** | 索引 `WITH(m=16,ef_construction=200)`;连接层 `SET hnsw.ef_search=200`(≥ 最大 LIMIT) | ✅ |
| Q2 | improve | **多路召回串行 `for`**:总延迟 = Σ 各路(pgvector ANN + ONNX user 塔 + 全文检索 + N×Redis),任一路慢阻塞整请求;无单路超时 | 有界线程池 + `CompletableFuture` 单路超时(默认 150ms),超时/异常当空,HOT 兜底已有 | ✅ |
| Q3 | improve | **排序特征 N+1**:5 个 RankService 在候选循环内逐个 `itemFeatures(id)`=`HGETALL`,200~500 候选 = 数百次串行 Redis RTT | `FeatureService` 加批量 `itemFeatures(Collection)`,`RedisFeatureService` 用 pipeline 一次拉全量 | ✅ |
| Q4 | improve | **Gemini `RestClient` 零超时**:`EmbeddingConfig` 两个 `RestClient.build()` 无超时→上游挂起无限阻塞搜索/query 主链路(降级兜"失败"兜不住"慢") | connect 2s / read 3s,超时抛出走本地 BGE 降级 | ✅ |
| Q5 | improve | 缺索引 + 明文密码:`bidword.ad_id`(多处全表扫)、`user_behavior(user_id,action,item_id)` 覆盖索引(已看过滤热点)、`sharding.yaml` 硬编码 `password:recsys` | 补索引 DDL(ds_0/ds_1 两库)+ 密码走 `${PG_PASSWORD}` | ✅ |

> **第一批已全部落地并通过全量 `mvn clean install`(16 模块 BUILD SUCCESS)。** 生效说明见下方「落地备注」。

### 落地备注(第一批)

- **Q1 对已有库需手动重建索引**:`CREATE INDEX IF NOT EXISTS` 不会改动既存索引;老 `pgdata` 卷需 `DROP INDEX idx_item_embedding_hnsw;` 后重跑 DDL 才吃到新 `m/ef_construction`。而 `SET hnsw.ef_search=200`(rec-engine Hikari `connection-init-sql`)对新旧库**立即生效**,是召回质量的关键项。
- **Q2 可回退**:`recsys.recall.parallel.enabled=false` 退回串行;`timeout-ms`(默认 150)、`pool-size`(默认 12)可调。合并结果顺序与串行完全一致(确定性)。
- **Q3 契约兼容**:`FeatureService.itemFeatures(Collection)` 是 default 方法(旧实现自动逐个兜底),仅 `RedisFeatureService` 覆盖为 pipeline;pipeline 异常自动回退逐个读。
- **Q5 pool**:顺带给 rec-engine 显式配 Hikari(`max=20/min=5/connectionTimeout=3s`),消除默认 10 连接在多路并发下的连接饥饿。

---

## 2. 第二批 · 结构性(M,决定能否走向生产规模)

| # | 类型 | 建议 | 为什么 | 状态 |
|---|---|---|---|---|
| S1 | extend | **补粗排层(pre-rank)** | 召回几百候选**直接全量精排**(`RankRouter` 无截断),规模上万即崩。用轻量线性打分(召回分+热度+类目亲和)砍到 top-K 再精排,插在 recall→rank 之间 | ✅ |
| S2 | improve | **排序特征扩充(5 维→8 维)** | 5 维是排序模型的共同天花板。已加 3 个交叉/离散度特征(`user_cat_cnt_norm` 类目参与深度 / `user_cat_ratio` 类目占比 / `item_rating_std` 评分共识度),**schema 驱动、向后兼容**:`FeatureAssembler` 保留默认 5 维 + 超集 + `assemble(order)`,深度模型经 `SparseFeatureEncoder.denseOrder()` 用各自 `dense_order`;在线/离线同源(`AsOfFeatureBuilder`+`BuildFeaturesJob`)、`gen-samples[-mt]`/4 训练脚本 `--extended-features`。**已验证**:默认 5 维输出逐位不变、扩展 8 维追加正确、旧模型零影响;重训扩维即生效 | ✅ |
| S3 | improve | **弹性护栏 Resilience4j** | 超时已由 Q1/Q4/Q5 覆盖(Hikari/RestClient);本项补**熔断器**:`GeminiEmbeddingClient.embedText/embedImage` 加 `@CircuitBreaker`,连续失败/超时达阈值即熔断、快速失败到降级(纯词法),半开自动探活。上层已 catch 降级,零行为回归。`recsys.fusion` 外的 `resilience4j.circuitbreaker` 配置。🟡 pgvector/Redis 熔断可后续同法扩展 | ✅(embedding) |
| S4 | improve | **网关路由补全** | 已补:rec-engine 全部入口(search/search-ads/feed/query/user/ad/*)路由到 8081(此前仅 recommend,其余落 web:8090 不可达);`/api/ad/**` 与 `/api/advertiser/**` 不重叠。🟡 `RequestRateLimiter` 限流可后续 | ✅ |
| S5 | improve | **配置热更新落地** | 已做轻量方案:`DynamicTuningService` 周期(30s)读 Redis Hash `recsys:tuning` 叠加静态 yml;融合旋钮(pop-debias/calibration/ftrl 的开关+权重)读覆盖层,`redis-cli hset recsys:tuning fusion.pop-debias.beta 0.8` 即热更免重启;缺则回退默认,零风险。可平滑替 Nacos | ✅ |
| S6 | improve | **负样本升级** | 已做两半:`train_two_tower.py` in-batch sampled-softmax **logQ 修正**(减 log P(item),去热门=假负偏置,`--no-logq` 关);`gen-samples-mt --hard-neg-ratio` 采**同类目 hard negative**(比纯热度随机更难、更有信息量) | ✅ |
| S7 | improve | **日志表分区 + 覆盖索引** | 已做:`05_partitioning.sql` —— `ad_event` 覆盖索引 `(event_type,request_id,ad_id)`+`(ad_id,creative_id)`(幂等、已对活库验证)+ `user_behavior`/`ad_event` 月度 RANGE 分区迁移模板(create-copy-swap,含 DETACH 归档运维) | ✅ |

---

## 3. 第三批 · 扩展新能力(算法 + 工程,中长期)

**算法**(✅ 已落地 4 项)
- ✅ **流行度去偏**:融合分 × `1/(1+item_pop_norm)^β`(`recsys.fusion.pop-debias`),从机制上替代 channel-boost 补丁。
- ✅ **`user_embedding` 时间衰减**:`UserEmbeddingJob` 正反馈向量按半衰期 `0.5^(age/halfLife)` 加权(`--half-life-days`,默认180),让用户向量偏向近期兴趣,修 VECTOR 召回"纯长期、无新鲜度"。
- ✅ **推荐精排分数校准**:离线 `rec-calibrate` 重跑 recall→rank 于时间切分留出集,拟合 rankScore→真实正反馈率的保序回归 → `rec:calib:{model}`;在线 `RecScoreCalibrator` 在融合前把 rank 分校准成可比概率(`recsys.fusion.calibration`),让 recall+rank 融合量纲一致(isotonic 单调,不改单策略内排序)。
- ✅ **多目标融合权重自动搜参**:离线 `tune-fusion` 重跑一次 recall→rank(mmoe/din 已把 pCTR/pCVR 写进快照),再对权重网格做纯内存重打分 + NDCG@K,搜出最优 `cvrBias/cvrWeight` 并写 CSV(把拍脑袋改成数据驱动;`RANK_STRATEGY=mmoe` 启动)。
- ✅ **PLE 替 MMoE**:`train_ple.py`(单层 CGC:共享专家 + 每任务专属专家 + 定制门控治多任务跷跷板,与 MMoE 同 ESMM/PAL/ONNX 契约)→ `model_ple.onnx`;在线 `PleRankService`(`strategy=ple`,serving 与 MMoE 同形)+ `RankRouter` ple 路由,可 `--rank-strategy=ple` 与 MMoE 对比。
- ✅ **冷启动 contextual bandit**:COLD 召回从"跨类目均匀铺开"升级为类目 **UCB** 驱动(`ColdStartBandit`:经验正反馈率 exploit + 欠曝光 explore,与广告 `ExplorationService` 同 UCB 家族);统计由离线 `cold-bandit-stats` 物化 `cold:cat:{category}`;关闭/无统计退回旧行为,下游强多样性重排仍保证覆盖(`recsys.recall.cold-bandit`)。
- ✅ **近线增量学习(FTRL)**:`FtrlProximal`(FTRL-Proximal LR,稀疏状态 + L1/L2)+ 离线 `train-ftrl`(可 `--incremental` 从 `ftrl:state` warm-start,负采样)近线刷新协同 CTR 模型 → `ftrl:weights`;在线 `FtrlScorer` 在融合加 `weight·pFtrl(user,item)`;哈希契约 `FtrlHashing`(在线/离线一致)。**已验证学习有效**(p(u,i) 训练后 0.50→0.72,负例 0.45)。`recsys.fusion.ftrl`。
- ✅ **多模态海报向量**:`GeminiEmbeddingClient.embedImage`(多模态 inlineData,同 768 维/空间)+ 离线 `backfill-multimodal`(海报图向量与文本向量 `α·text+(1-α)·image` 融合写回 `item_embedding`,pgvector 召回零改动);无海报/不支持多模态则优雅跳过。需海报图数据(MovieLens 用 tmdbId 拉 TMDB)。
- ✅ **语义 ID 当精排稀疏特征**:`SparseFeatureEncoder` schema 驱动、向后兼容地在 (user,item,cat) 后追加 RQ-VAE 语义 ID (c0,c1,c2)(**已验证**:无声明 width=3 不变、启用 width=6 追加、缺失补 0);`train_deepfm.py --semantic-id` 读 `item_semantic_id.csv` 训练并导出 `semantic_id_map.json`+schema 标志。DeepFM 按 `n_fields` 泛化,rank 服务 `sparse[i]=encode(...)` 透传变宽,**在线零改动**;需重训激活。

**工程**
- **测试体系 0→1**:优先在线/离线特征一致性 golden-file 契约测试(`FeatureAssembler`/`SparseFeatureEncoder`/`SequenceEncoder`/`BgeTokenizer`/`BidwordInvCodec` 全是纯函数极易测)+ 核心纯函数单测 + Testcontainers 集成测试 + jacoco 门槛。
- **最小 CI**(GitHub Actions:build + test gate + ONNX 单文件自包含校验,卡"陈旧 target/classes"事故)。
- ✅ **告警规则 + SLO**:`monitoring/alert-rules.yml`(11 条规则,**promtool 验证通过**)——SLI 记录规则(P99 延迟/错误率/缓存命中/模型回退率)+ 可用性 SLO 多窗口 **burn-rate** 告警(快窗 page/慢窗 ticket)+ 延迟/宕机/回退/空召回告警;`alertmanager.yml` + compose obs 加 alertmanager 服务;prometheus 载入规则。SLO:可用性 99.5%、P99<200ms。
- ✅ **结构化日志 + traceId + 分布式追踪**:`micrometer-tracing-bridge-otel` 自动把 traceId/spanId 注入 MDC;`logback-spring.xml`(默认可读控制台带 trace/user、`json` profile 出 LogstashEncoder JSON 进 ELK/Loki);编排把 userId/scene 放 MDC;`management.tracing.sampling` 采样(加 OTLP exporter + Tempo/Jaeger 即导出 span)。
- **离线作业调度编排 DAG**(25 个 Job 全靠手动 `--job`,断一环下游静默用陈旧数据;Airflow/Argo 或轻量 `@Scheduled`+分布式锁)。
- **容器化 + k8s + 自定义健康探针**(无 Dockerfile;`ModelReadinessHealthIndicator` 让模型未加载则 readiness 不通过)。
- **数据质量/漂移监控**(embedding 覆盖率/PSI、特征分布偏移、pCTR vs 实际 CTR 校准偏差)。

---

## 4. 微服务拆分:真实阻碍与路径(不要 16 模块拆 16 服务)

**四个硬阻碍**:① 共享单库 + 共享 DataSource;② 进程内方法调用无网络契约(RecallService/RankService 是 Java 接口);③ ONNX 模型打进 jar(部署与构建强耦合);④ `recsys-common` 上帝模块全局波及。

**渐进路径**:第 0 步先做治理(Q2/Q3/S3/S4,无论拆不拆都需要)→ 第 1 步剥离 `recsys-embedding`(无状态重算力,依赖最少)→ 第 2 步 `recsys-advertiser`(写侧已天然独立)→ 第 3 步 recall/rank 独立推理服务(需先解决阻碍②③,成本最高,仅在独立扩缩诉求出现时做)。**当前负载下,单体优先仍是正确选择。**

---

*本路线随实施推进更新状态列。*
